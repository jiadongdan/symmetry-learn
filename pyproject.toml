[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "symmetry-learn"
version = "0.1.0"
description = "Deep learning for STEM images by symmetry"
readme = "README.md"
authors = [{ name = "Jiadong Dan" }]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
    "torch>=2.0",
    "ase",
    "matplotlib",
    "scikit-image",
    "scikit-learn",
    "spglib",
    "psutil",
    "symmetry-learn-default-model==1.0.0",
]

[project.urls]
Homepage = "https://github.com/jiadongdan/symmetry-learn"
Repository = "https://github.com/jiadongdan/symmetry-learn"
Issues = "https://github.com/jiadongdan/symmetry-learn/issues"

[project.optional-dependencies]
test = ["pytest"]
torch = []
provider = []
inference = []

[project.scripts]
symmetry-learn-provider = "symmlearn.provider.worker:main"

[tool.pytest.ini_options]
markers = [
    "torch: tests that require torch and the optional torch workflow",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["symmlearn*"]
